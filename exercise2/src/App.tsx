import React, {FC} from "react";
import Counter from "./components/Counter"; //importing counter component

const App: FC = () => {
    return (
        <div>
            <Counter />
        </div>
    );
};

export default App;
